#include <cstdio>

class Date{
	private:
		int day, int month, year;
	public:
		// Default constructor
		Date() : day(1), month(1), year(1980){

		}

		// Parameterized constructor
		Date(int inti_day, int init_month, int init_year) : day(init_day),
		month(init_month),
		year(init_year){
		}
}

